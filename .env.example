# 环境变量配置说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 修改下面的配置值为您实际的配置
# 3. 确保 .env 文件不会被提交到版本控制系统（已添加到 .gitignore）

# ==================== 开发环境配置 ====================
# 开发环境使用本地数据库和缓存服务

# ==================== 数据库配置 ====================
# 数据库连接URL，请根据实际数据库信息修改
# 格式：postgresql://用户名:密码@主机:端口/数据库名
# 示例：postgresql://guoxue_user:secure_password@localhost:5432/guoxue_db
DATABASE_URL=postgresql://user:password@localhost:5432/guoxue

# Redis连接URL，请根据实际Redis信息修改
# 格式：redis://用户名:密码@主机:端口/数据库编号
# 示例：redis://default:redis_password@localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# ==================== JWT配置 ====================
# JWT密钥 - 用于签名JWT令牌，必须修改为强随机字符串
# 生成方法：openssl rand -base64 32
# 或使用Python：import secrets; print(secrets.token_urlsafe(32))
JWT_SECRET_KEY=your_secret_key_here

# JWT算法，通常使用HS256
JWT_ALGORITHM=HS256

# 访问令牌过期时间（分钟）
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== AI服务配置 ====================
# DeepSeek API密钥 - 从DeepSeek官网获取
# 必须修改为实际的API密钥才能使用AI功能
DEEPSEEK_API_KEY=your_deepseek_api_key

# Chroma向量数据库路径
CHROMA_DB_PATH=./chroma_db

# ==================== 微信小程序配置 ====================
# 微信小程序AppID和AppSecret - 从微信公众平台获取
# 必须修改为实际的小程序凭证才能使用微信登录功能
WECHAT_APP_ID=your_wechat_app_id
WECHAT_APP_SECRET=your_wechat_app_secret

# ==================== 支付配置 ====================
# 支付网关密钥 - 从支付服务商获取（如微信支付、支付宝等）
# 如果不需要支付功能可以保持默认值，但上线前建议配置
PAYMENT_GATEWAY_KEY=your_payment_gateway_key

# ==================== Redis配置 ====================
# Redis服务器配置（与REDIS_URL中的信息保持一致）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# ==================== MinIO配置 ====================
# MinIO对象存储配置 - 用于文件存储
# 如果不需要文件上传功能可以保持默认值
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=your_minio_access_key
MINIO_SECRET_KEY=your_minio_secret_key

# ==================== 日志配置 ====================
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径
LOG_FILE=./logs/app.log

# ==================== 安全配置 ====================
# Django SECRET_KEY - 用于加密签名，必须修改为强随机字符串
# 生成方法：openssl rand -base64 32
# 或使用Python：import secrets; print(secrets.token_urlsafe(32))
SECRET_KEY=your_secret_key_here

# 允许的主机列表，用于安全验证
ALLOWED_HOSTS=localhost,127.0.0.1

# ==================== 端口配置 ====================
# 后端服务端口
BACKEND_PORT=8000

# 前端服务端口
FRONTEND_PORT=3000

# ==================== 其他配置 ====================
# 调试模式，生产环境应设置为False
DEBUG=True

# 运行环境标识
ENVIRONMENT=development

# ==================== 新用户配置 ====================
# 新用户注册时赠送的token数量（1000 tokens = 10元）
NEW_USER_TOKENS=1000